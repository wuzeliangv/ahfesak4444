# Lambda deployment package — only runtime deps, no dev tools.
# boto3/botocore are provided by the Lambda Python 3.12 runtime, but we pin them
# here so local sam invoke and AWS Lambda use identical versions, avoiding
# "works on my machine" drift.

boto3>=1.35.0,<2.0.0
botocore>=1.35.0,<2.0.0
aioboto3>=13.2.0,<16.0.0
cryptography>=43.0.0,<50.0.0
PySocks>=1.7.1
